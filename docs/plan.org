#+TITLE: Planner
#+TODO: TODO(t) PLANNED(p) WORKING(w) | DONE(d)
#+OPTIONS: ^:{}

* Tracks
**  UI [0/8]
*** TODO Pause menu
*** TODO Progression menu
*** TODO Upgrade menu
*** WORKING Control menu
+ Two-tier statusbar for slingshot payload selection. Design: [[file:ui-control-menu.org]],
  decisions: [[file:adr/0003-control-menu.org]], vocab: [[file:glossary.org]].
+ Tier-1 tabs by entity kind (Direct/Emitter/Hardware) -> Slingshot_Output union. Tier-2 contextual:
  celestial picker (Direct), celestial + Emitter_Preset picker (Emitter), disabled (Hardware).
+ Wires available_objects (read for the first time) + makes emitter subsystem reachable.
+ Repurpose dead Render_LaunchMenu struct into Control_Menu state. Render in ui.odin, hit-test in
  input.odin. Introduces Drag_Intent enum + Parameters_Emitter_Preset table.
*** TODO Launch menu
+ Object + emitter selection for the slingshot. Now covered by the Control menu track above; keep
  this entry for any separate launch-confirmation UI if it emerges.
*** TODO Special Hardware entities + radius drag
+ Deferred from Control menu (ADR 0003). Implement Collector Drone / Gravity Satellite components,
  the Radius Drag_Intent path in sys_slingshot + preview render, and placement. Enable the Hardware
  tab (currently a disabled stub) and the Slingshot_Output_Hardware variant.
*** TODO Keyboard controls for control menu
+ Deferred from Control menu (ui-control-menu.org). Mouse-only for now; expose keyboard bindings for
  tab/payload/preset selection to retain game pace.
*** TODO Emitter preset balance pass
+ Deferred from ADR 0003. Tune Emitter_Preset values (Burst/Steady/Sustained/Trickle) and the
  base_cost preset_cost_multiplier once the menu ships and playtests.
** Economy [0/2]
*** TODO Scoring system
*** TODO Launch cost
** Progression [1/7]
*** TODO Modifier system for upgrades
*** TODO Prestige (supernova reset)
+ Phase 3 from docs/overview.org. When the star becomes too massive for orbits to escape, the system
  collapses into a black hole/supernova: soft reset that grants premium currency and permanent
  multipliers for the next run. Needs a star-mass threshold trigger, reset flow, and currency
  plumbing.
*** DONE Save/load persistence
+ Full Game state, single slot, binary format, native only. Resume-like-paused on launch.
  Detailed plan: [[file:plan/serialization.org]], decisions: [[file:adr/0001-serialization.org]]
+ Implemented: buffer-based serialize/deserialize, atomic disk write, autosave 30s, exit save,
  7 new tests green. Pending: manual acceptance run (quit/relaunch resume, kill -9, corrupt save).
*** TODO Prestige fields in save format
+ Bump SAVE_VERSION and extend the payload when prestige lands. Deferred from serialization.org.
*** TODO Pause-menu manual save button
+ Deferred from serialization.org. Autosave + exit save cover the basics; manual trigger is UX polish.
*** TODO WASM persistence backend
+ IndexedDB/localStorage via emscripten. Native file save lands first (serialization.org).
*** TODO Save format migration system
+ Needed post-release. Dev builds reject mismatched versions and start fresh.
** Infrastructure [1/2]
*** DONE Dedicated log file
+ No logging exists yet; failures (e.g. persistence save/load) are silent. Set up a game logger
  that writes to a log file (next to save.bin in the OS user data dir), then route warnings/errors
  through it. Inline logging via fmt/eprintln etc. is not allowed anywhere in the codebase.
*** TODO Log level release filter
+ Change runtime log filter to .Warning or .Error in production release. Currently set to .Debug for dev.
** Physics [0/1]
*** TODO Decouple visual and simulation parameters
** Testing [2/3]
*** DONE Decouple logic from render/globals for testability
+ Moved delete_entities/mass_delta globals into Game struct.
+ Extracted sys_slingshot_compute_preview from sys_render; render is draw-only.
*** DONE Rewrite test suite from scratch
+ Old suite discarded (coupled to live balance params).
+ 51 tests, all green: fixtures (pinned params, builders, test_step harness), domain asserts,
  ecs/utils/system matrix + scenario tests.
*** TODO CI for test suite
+ GitHub Action: setup Odin + make test. Red suite should block merges.
** Bugs [2/5]
*** TODO Fix build for WASM
*** DONE Shockwave scale is broken
+ Even for simpler collisions, the shockwave scale is massive and results in it covering the whole screen. The energy fragments generated are also too small in size and cover the whole scren.
+ Switched shockwave growth from sqrt to log scaling with max cap (20px/s). Increased fragment size (6px), clamped count (3-10), fixed 17px spread radius. Reduced drift frequency/amplitude to ~23% for gentle floating motion.
*** DONE Floating energy animations are broken
+ Energy blobs float all over the screen due to framerate issues?
*** TODO sys_slingshot calls sys_lifecycle_spawn_shockwave directly
+ Cross-system call. Systems should not call each other; move to event queue instead.
*** DONE Fragment energy was not conserved on shatter
** Backlog
*** TODO Energy-dependent fragment size and spread
+ Fragment size (ENERGY_FRAGMENTS_SIZE) and spread radius (ENERGY_FRAGMENTS_SPREAD_RADIUS) are currently fixed constants. As bigger objects (Jupiter-sized planets etc.) enter the game, these should scale with collision energy so fragments remain proportional and collectible.
